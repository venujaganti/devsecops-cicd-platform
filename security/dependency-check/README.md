# OWASP Dependency-Check

OWASP Dependency-Check is used to identify publicly disclosed
vulnerabilities in project dependencies.

## Project Structure

```text
dependency-check/
├── dependency-check.sh
└── README.md
```

The `dependency-check.sh` script provides a repeatable dependency security scan workflow.
